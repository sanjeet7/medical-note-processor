# =============================================================================
# Medical Note Processor - Environment Configuration
# =============================================================================
# Copy this file to .env and configure:
#   cp .env.example .env
#
# Then choose ONE of the scenarios below and fill in your API key(s).

# =============================================================================
# SCENARIO A: SAME Provider for LLM + Embeddings. Example: OpenAI for Everything (Recommended - Simplest Setup)
# =============================================================================
# Just set your OpenAI API key - everything uses it
LLM_API_KEY=sk-your-openai-api-key-here  
LLM_PROVIDER=openai 
LLM_MODEL=gpt-5.1
# Cheaper model for metadata extraction (falls back to LLM_MODEL if empty)
METADATA_LLM_MODEL=gpt-5.1-mini
EMBEDDING_PROVIDER=openai 
EMBEDDING_MODEL=text-embedding-3-small 


# =============================================================================
# SCENARIO B: DIFFERENT Provider for LLM + Embeddings. Example: Anthropic for LLM + Cohere for Embeddings
# =============================================================================
# Uncomment below and comment out Scenario A above
# LLM_API_KEY=sk-ant-your-anthropic-api-key-here
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-sonnet-4-5-20250929
# EMBEDDING_API_KEY=sk-your-openai-api-key-here  # REQUIRED! Embeddings need separate key
# EMBEDDING_PROVIDER=cohere
# EMBEDDING_MODEL=embed-english-v3.0


# Force re-seed database and re-index on startup (default: false)
# FORCE_INIT=true
